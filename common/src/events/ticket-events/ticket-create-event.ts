import { Subjects } from "../Subjects";

export interface TicketCreateEvent {
  subject: Subjects.TicketCreated;
  data: {
    id: string;
    title: string;
    price: number;
    userId: string;
  };
}
